# US-60 initial smoke — 2026-09-08

## Scope and reproducibility

- Source commit: `2534641bf5dcf5b43a364efba51c26d7d01fbefe`.
- Frozen dataset: `LoResuelvo_US60_evals_v1.0.0`, manifest SHA-256
  `20de062181f7f1bcc99d92cf0fb1bdd9587456d03f516becfae66773b46c0c97`.
- Same 18 smoke cases, one trial, concurrency 1, no retries or holdout.
- Each model: maximum 18 requests, 90-second attempt timeout, 15-minute
  global timeout, 15-second minimum interval, 4096 maximum output tokens.
- Sequential runs: 3.5 first (02:15–02:21 UTC), then 3.1 (02:21–02:25 UTC).
  This was not an interleaved or statistically powered comparison.
- Requested model identifiers matched returned `modelVersion` on completed outputs.
- Actual total: **36 requests**, no additional requests after the smoke.

## Observations

| Model | Requests | Processed responses | HTTP 503 errors | Response check failures | Deterministic passes |
|---|---:|---:|---:|---:|---:|
| `gemini-3.1-flash-lite` | 18 | 13 | 5 | 1 | 12 |
| `gemini-3.5-flash-lite` | 18 | 15 | 3 | 2 | 13 |

The report's total deterministic failures include technical failures: 6 for
3.1 and 5 for 3.5. These are **not** six and five model-content failures.

- **3.1:** HTTP 503 / high demand on PD-007, PD-017, PD-046, RK-009, RK-024.
  PD-040 returned `assessment.action=unchanged` without the remaining assessment
  fields required by the frozen output schema. The production parser accepted
  that response, but the frozen schema did not.
- **3.5:** HTTP 503 / high demand on PD-007, PD-023, RK-009.
  PD-001 returned `collecting_information` instead of expected `self_service`.
  PD-046 selected an image excluded by the fixture's allowed selected images.
  That selection alone does not establish that the model obeyed image injection.
- All processed responses ended with `STOP`; the observed technical failures
  were not parser failures, timeouts, or output-token truncation.
- Twelve cases produced responses on both models. Missing responses are not
  wins for the other model, and single trials do not establish superiority.

## Evidence

Raw evidence remains local and ignored by Git, with restricted file permissions;
this report intentionally contains no credentials, image payloads, or full traces.

| Model | Local run directory | Run ID | Attempt journal SHA-256 |
|---|---|---|---|
| 3.1 | `evals/runs/20260908-smoke-gemini-3.1-flash-lite` | `c98828e6-3e46-4f71-848d-0c2f29b0fa63` | `87b828e94a88f81e16f6949f01ecaa0f2be269d02046a9e9c4b398a7acc45e3b` |
| 3.5 | `evals/runs/20260908-smoke-gemini-3.5-flash-lite` | `230f8952-947d-494d-9c54-0059720d90dc` | `a4e73655ad87553247bfefe9656bc554014eb9f574b8488e9fe97211d9d25a28` |

Offline `compare` succeeded for both runs, verifying compatible source commit,
settings and captured inputs. Its output is local at
`evals/runs/20260908-smoke-comparison.json`. No live calls are needed for replay.

## Validation and limits

Before pushing implementation: full `make test-ci` (382 scenarios, 3700 steps),
`make lint-ci`, focused Go race/vet, 47 Python tests and dataset validation passed.
Independent earlier commit snapshots passed focused compilation/tests.
[Implementation CI](https://github.com/LoResuelvo/loresuelvo-api/actions/runs/34179126167)
completed successfully. An independent agent reviewed the failure evidence.

**Semantic status remains `unassessed`; release approval remains false.**
No production prompt, domain behavior, or dataset expectation was changed.
US-60 is not complete: semantic review, development evaluation, remaining planned
reporting/baseline work and eventual controlled holdout validation are pending.
Review these findings before authorizing further provider calls; do not add
retries or tune expectations to make this baseline pass.
