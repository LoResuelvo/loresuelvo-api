type: object
required:
  - payment_intent_id
  - status
  - checkout_url
  - expires_on
  - pricing
properties:
  payment_intent_id:
    type: string
    format: uuid
    description: Internal payment intent that tracks this booking deposit.
  status:
    type: string
    enum:
      - checkout_ready
  checkout_url:
    type: string
    format: uri
    description: Mercado Pago Checkout Pro URL to which the frontend redirects the consumer.
  expires_on:
    type: string
    format: date-time
    description: Checkout expiration, capped by the proposal booking-payment deadline.
  pricing:
    $ref: '#/components/schemas/BookingTerms'
additionalProperties: false
