description: Complete authenticated Consumer profile.
allOf:
- $ref: '#/components/schemas/BaseCurrentUserResponse'
- type: object
  required:
  - role
  properties:
    role:
      type: string
      enum:
      - consumer
