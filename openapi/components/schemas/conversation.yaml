type: object
description: Conversation created for a provider work request.
required:
- id
- status
properties:
  id:
    type: integer
    format: int32
    description: Conversation identifier.
    example: 1
  status:
    type: string
    description: Current conversation status.
    enum:
    - pending
    example: pending
additionalProperties: false
