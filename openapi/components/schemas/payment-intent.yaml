type: object
required:
  - id
  - status
properties:
  id:
    type: string
    format: uuid
  status:
    type: string
    description: Current result of the verified payment flow.
    enum:
      - requires_checkout
      - checkout_ready
      - processing
      - paid
      - rejected
additionalProperties: false
