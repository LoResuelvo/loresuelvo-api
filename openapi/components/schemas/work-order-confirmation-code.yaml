type: object
description: Consumer-visible credential used later to authorize confirmation that the service was performed.
required:
  - confirmation_code
properties:
  confirmation_code:
    type: string
    pattern: '^[0-9]{4}$'
    example: '0042'
additionalProperties: false
